# Media Search Engine - Environment Variables Configuration

# ==============================================================================
# Supabase Configuration (REQUIRED for production use)
# ==============================================================================
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Your Supabase API key (anon/public key or service_role key)
SUPABASE_KEY=your-supabase-key-here

# ==============================================================================
# Embedding Model Configuration (OPTIONAL)
# ==============================================================================
# Select which AI model to use for embeddings
# Available options:
#   - clip-vit-base-patch32 (default, balanced speed/quality, 512 dims)
#   - clip-vit-large-patch14 (higher quality, 768 dims)
#   - siglip-base (multilingual support, 768 dims)
#
# Default: clip-vit-base-patch32
# MODEL_KEY=clip-vit-base-patch32

# ==============================================================================
# Application Configuration (OPTIONAL)
# ==============================================================================
# Port for the application (default: 8080)
# PORT=8080

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# ==============================================================================
# Development Notes
# ==============================================================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. For local development without Supabase, the app will run in demo mode
#    with limited functionality (embedding model will still work)
# 4. Railway/production deployments should set these as environment variables
#    in the platform's dashboard
